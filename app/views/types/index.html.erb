<div class="row content">
  <div class="small-9 columns">
    <%= render 'shared/flasher' %>
      <table>
        <thead>
          <tr>
            <th>Id</th>
            <th>Name lv</th>
            <th>Name ru</th>
            <th>Slug</th>
            <th>#</th>
            <th></th>
            <th></th>
          </tr>
        </thead>

      <% @types.each do |type| %>

      <tbody>
        <tr>
          <td><%= type.id %></td>
          <td><%= type.type_name_lv %></td>
          <td><%= type.type_name_ru %></td>
          <td><%= type.type_slug %></td>
          <td><%= type.places.all.size %></td>
          <td><%= link_to 'Edit', edit_type_path(type) %></td>
          <td><%= link_to 'Destroy', type, method: :delete, data: { confirm: 'Are you sure?' } if current_user.id == 1 %></td>
        </tr>
        </tbody>
      <% end %>
      </table>
    </div>
  <div class="small-3 columns">
        <%= render 'shared/admin/admin_panel' %>
    <%= render 'shared/navigation_panel'%>
    <%= render 'shared/login' %>
  </div>  
</div>