<h4>More like this</h4>
<ul class="disc margin-left panel recent">
	<% same_type = get_like_this(@place) %>


  <% same_type.each do |place| %>
    <li><%= link_to place.name, place %></li>
  <% end %>

</ul>