<% provide(:title, t(:home) ) %>
<%= metamagic :description => "#{t(:meta_default_description)}", :keywords => "#{t(:meta_default_keywords)}" %>

<div class="row content content_bottom_margin">
  <div class="large-12 small-12 columns" >
   

    
    <div class="row content">
      <div  class="large-9 small-12 columns">
        <%= render 'shared/flasher' %>
        <% if @json %>
          <%= render 'shared/gmap' %>
          <br>
        <% else %>
          <%= render 'shared/promotion' %>
          <br>
          <br>
          <div class="label radius success"><%= t(:recent_added_places) %></div>
        <% end  %>
          
        
        <% unless @type_vip.blank?  %>
          <%= render 'vip'%>
        <% end %>

        <div id="places">
              
          <br>
          <%= render @places %>
       </div>

        <div id="paginator">
          <%= paginate @places %>
        </div>
      </div>

      <div class="large-3 columns hide-for-small">


        <%= render 'shared/admin/admin_panel' %>
        <%= render 'shared/navigator' %>
        <%= render 'shared/buttons' %>
        <%= render 'shared/login' %>
        <%= render 'shared/blog_panel'%>
        <%# render 'sponsor' %>

       </div>

    </div>
  </div>

</div> 



