<nav class="top-bar" >

		<ul class="title-area">
		  <li class="name"><h1><%= link_to "Yoda.lv", root_path %></h1></li>
		  <li class="toggle-topbar menu-icon"><a href="#"><span>menu</span></a></li>
		</ul>

		<section class="top-bar-section">
			<ul class="right">
				<li class="<%= 'active' if current_page?(:controller => 'places', :action => 'index') %> valid">
				<%= link_to t(:home), root_path %>
				</li>
				<li class="divider"></li>
				<li class="<%= 'active' if current_page?(:controller => 'posts', :action => 'index') %> valid">
				<%= link_to t(:blog), posts_path %></li>
				<li class="divider"></li>
				<li class="<%= 'active' if current_page?(:controller => 'requests', :action => 'new') %> valid">
	              <%= link_to t(:add_new_place), new_request_path %>
	            </li>

	            <li class="divider"></li>
				<li class="<%= 'active' if current_page?(:controller => 'contacts', :action => 'new') %> valid">
	              <%= link_to t(:contact_with_us), :controller => :contacts, :action => :new %>
	            </li>
	            <li class="divider"></li>
	            <li class="<%= 'active' if current_page?(subscribe_path) %> valid" >
					<%= link_to t(:subscribe), subscribe_path %>
	            </li>
	            <li class="divider"></li>
				<%= render 'shared/language_selector'  %>
			</ul>
		</section>
		
	</nav>
