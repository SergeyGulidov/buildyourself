<dl class="sub-nav">
  <dd class="active"><a href="#">With review</a></dd>
  <dd class="active"><a href="#">4 comments</a></dd>
  

    <dd class="active"><%= pluralize place.votes, "vote" %></dd>
    <% if current_user && current_user.can_vote_for?(place) %>
      | <%= link_to "up", vote_place_path(place, value: 1), method: "post" %>
      | <%= link_to "down", vote_place_path(place, value: -1), method: "post" %>
    <% end %>


  <dd class="stars"> <i class="general foundicon-star">
  		<i class="general foundicon-star">
  			<i class="general foundicon-star">
  				<i class="general foundicon-star">
  					<i class="general foundicon-star">
  					</i></i></i></i></i>
  	</dd>
</dl>