<%= simple_form_for(@interval) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :interval_name %>
    <%= f.input :interval_slug %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
